{
  "name": "airdrop-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "Crypto airdrop allocation + merkle generation pipeline",
  "scripts": {
    "build:allocation": "python3 scripts/build_allocation.py --input-csv sample_input.csv",
    "build:merkle": "node scripts/generate_merkle.js --input final_allocation.csv --output merkle.json"
  },
  "dependencies": {
    "csv-parse": "^5.5.6",
    "ethers": "^6.15.0",
    "merkletreejs": "^0.6.0"
  }
}
